# NoevaPet Pre-Deployment V16

## Changes
- Replaced the Warenkorb hero photograph with a new, separate dog-and-cat lifestyle image.
- Filled the previously empty salmon label on the How-it-works hero with concise copy and lowered the visual slightly.
- Removed the homepage basket preview entirely when no pet profile exists. It appears only after a pet has been saved.
- Changed the empty navigation pet state from “Kein Tier / No pet” to the actionable “Tier hinzufügen / Add pet”.
- Fixed empty-pet state handling so deleting all profiles no longer silently recreates or targets Milo.
